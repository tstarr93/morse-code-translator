const input = document.querySelector("#input-field");

// Need to take what's written in the input-field


// Run it through a function -> Takes each letter and determines its Morse equivalent

// -> Returns each letter, translated, and put into a string

// Take that string and put it in result-field using innerHTML